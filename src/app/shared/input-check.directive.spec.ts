import { InputCheckDirective } from './input-check.directive';

describe('InputCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new InputCheckDirective();
    expect(directive).toBeTruthy();
  });
});
